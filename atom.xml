<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>一个涂鸦板</title>
  
  <link href="/atom.xml" rel="self"/>
  
  <link href="https://mahui.github.io/"/>
  <updated>2017-03-28T15:01:39.000Z</updated>
  <id>https://mahui.github.io/</id>
  
  <author>
    <name>马荟</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Netty用户指南4.x</title>
    <link href="https://mahui.github.io/2017/03/27/User-guide-for-4-x-CN/"/>
    <id>https://mahui.github.io/2017/03/27/User-guide-for-4-x-CN/</id>
    <published>2017-03-27T07:06:43.000Z</published>
    <updated>2017-03-28T15:01:39.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>Based on <a href="http://netty.io/wiki/user-guide-for-4.x.html" target="_blank" rel="external">User guide for 4.x</a> .</p>
</blockquote>
<h1 id="序言-preface">序言 <a href="http://netty.io/wiki/user-guide-for-4.x.html#preface" target="_blank" rel="external">Preface</a></h1>
<h2 id="问题-the-problem">问题 <a href="http://netty.io/wiki/user-guide-for-4.x.html#the-problem" target="_blank" rel="external">The Problem</a></h2>
<p>如今我们使用通用的应用程序或者库来彼此通讯。比如，我们经常使用 HTTP 客户端库从远程服务器接收信息或者执行远程服务的调用。但无论如何，一个通用的协议或者它们的实现有时候可用性不是太好。这就是为什么有时候我们并不使用通用的 HTTP 协议来交换大数据、电子邮件信息以及金融数据和多人网络游戏数据这种近实时的数据。所需要的是为了一个专门的目的去实现一个高度优化的协议。比如，你可能需为了基于 AJAX 的聊天应用，媒体传输，或者大文件传输去实现一个优化的 HTTP 服务。你甚至可以为了你的需求去精确的定义并实现一个全新的协议。另外一个经常发生的情况是你必须去处理系统中遗留的专门协议去确保与旧系统的交互。这种情况下重要的是，我们如何能够快速的实现该协议，并且不会牺牲应用程序的稳定性和性能。</p>
<h2 id="解决方案-the-solution">解决方案 <a href="http://netty.io/wiki/user-guide-for-4.x.html#the-solution" target="_blank" rel="external">The Solution</a></h2>
<p><a href="http://netty.io" target="_blank" rel="external">Netty</a> 是一个提供了异步事件驱动 (asynchronous event-driven) 网络应用程序框架和工具的杰作，可以用来快速开发可维护的，高性能 · 高可用的服务端和客户端协议。</p>
<p>从另一方面来讲，Netty 是一个可以快速开发网络程序如客户端和服务端协议的 NIO 客户端/服务端框架。它大大的简化了如 TCP 和 UDP 这类套接字服务的网络应用程序的开发并优化了其开发流程。</p>
<p>“快速简单”并不意味着开发出的应用程序的可维护性变差或者遇到性能问题。 Netty 从大量的如 FTP、SMPT、HTTP 以及各种二进制文本传输协议的实现中获得经验，并精心设计。因此， Netty 成功的获得了可以快速开发，并且不妥协性能、稳定性以及灵活性的方法。</p>
<p>一些用户用户或者发现过其他一些网络应用框架也声称有以上优势，可能会想问 Netty 和它们有何不同。答案是，设计哲学。 Netty 从一开始就设计得为你提供舒适的 API 及实现。这不是什么可感知的事务，但你会意识到这些设计哲学让你的生活变得更简单，就像你开始阅读这份指南，并且开始轻松使用 Netty 。</p>
<h1 id="开始-getting-started">开始 <a href="http://netty.io/wiki/user-guide-for-4.x.html#getting-started" target="_blank" rel="external">Getting Started</a></h1>
<p>本章指南围绕着 Netty 的核心结构，据一些简单的例子，让你轻松开始。本章结束后，你将可在 Netty 之上编写客户端和服务端。</p>
<p>如果你学习东西的时候喜欢使用自上而下分析法，那么你也许可以从第二章，架构总览开始，然后回到这里。</p>
<h2 id="开始之前-before-getting-started">开始之前 <a href="https://github.com/netty/netty/wiki/User-guide-for-4.x#before-getting-started" target="_blank" rel="external">Before Getting Started</a></h2>
<p>要想运行本章中所引入的例子，需要两个最小的依赖：Netty 最新的稳定版 和 JDK 1.6 或者更新。Netty 最新的稳定版可以从 <a href="http://netty.io/downloads.html" target="_blank" rel="external">项目下载页面</a> 下载。可以从 JDK 供应商的网站下载正确版本的 JDK 。</p>
<p>在你阅读的过程中，你可能对本章中引入的类有一些疑问。如果你想知道它们可以参考 API 文档。为了你的方便，文档中所有的类名都是引用的在线 API 文档。当然了，如果本文档中有任何错误信息，以及语法和打印中的错误，或者你有什么完善本文档的好的建议，可以<a href="http://netty.io/community.html" target="_blank" rel="external">联系Netty项目社区</a>。</p>
<h2 id="写一个-discard-服务-writing-a-discard-server">写一个 Discard 服务 <a href="https://github.com/netty/netty/wiki/User-guide-for-4.x#writing-a-discard-server" target="_blank" rel="external">Writing a Discard Server</a></h2>
<p>世界上最简单的协议是 <a href="http://tools.ietf.org/html/rfc863" target="_blank" rel="external"><code>DISCARD</code></a> ，而不是 “Hello,World!”。该协议丢弃任何收到的数据而不做任何回应。
要实现 <code>DISCARD</code> 协议，你唯一要做的事情就是忽略任何收到的消息。让我们直接从一个 handler 的实现开始，handler 是 Netty 生成的用来处理 I/O 事件的。</p>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">package</span> io.netty.example.discard;</div><div class="line"></div><div class="line"><span class="keyword">import</span> io.netty.buffer.ByteBuf;</div><div class="line"></div><div class="line"><span class="keyword">import</span> io.netty.channel.ChannelHandlerContext;</div><div class="line"><span class="keyword">import</span> io.netty.channel.ChannelInboundHandlerAdapter;</div><div class="line"></div><div class="line"><span class="comment">/**</span></div><div class="line"> * 处理一个服务端的 channel。</div><div class="line"> */</div><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DiscardServerHandler</span> <span class="keyword">extends</span> <span class="title">ChannelInboundHandlerAdapter</span> </span>&#123; <span class="comment">// (1)</span></div><div class="line"></div><div class="line">    <span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">channelRead</span><span class="params">(ChannelHandlerContext ctx, Object msg)</span> </span>&#123; <span class="comment">// (2)</span></div><div class="line">        <span class="comment">// 默默的丢弃收到的数据。</span></div><div class="line">        ((ByteBuf) msg).release(); <span class="comment">// (3)</span></div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">exceptionCaught</span><span class="params">(ChannelHandlerContext ctx, Throwable cause)</span> </span>&#123; <span class="comment">// (4)</span></div><div class="line">        <span class="comment">// 当引发异常时关闭连接。</span></div><div class="line">        cause.printStackTrace();</div><div class="line">        ctx.close();</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<ol>
<li>
<p><code>DiscardServerHandler</code> 继承自 <a href="http://netty.io/4.0/api/io/netty/channel/ChannelInboundHandlerAdapter.html" target="_blank" rel="external"><code>ChannelInboundHandlerAdapter</code></a>，该类实现了 <a href="http://netty.io/4.0/api/io/netty/channel/ChannelInboundHandler.html" target="_blank" rel="external"><code>ChannelInboundHandler</code> </a>.<a href="http://netty.io/4.0/api/io/netty/channel/ChannelInboundHandler.html" target="_blank" rel="external"><code>ChannelInboundHandler</code></a> 接口，提供了多种事件处理方法，你可以重写这些方法。现在，你只需要继承 <a href="http://netty.io/4.0/api/io/netty/channel/ChannelInboundHandlerAdapter.html" target="_blank" rel="external"><code>ChannelInboundHandlerAdapter</code></a> ,而无需自己实现接口中的处理方法。</p>
</li>
<li>
<p>我们重写了 <code>channelRead()</code> 方法。无论何时从客户端收到消息时，该方法被调用。在这个例子中，收到的消息类型是 <a href="http://netty.io/4.0/api/io/netty/buffer/ByteBuf.html" target="_blank" rel="external"><code>ByteBuf</code></a></p>
</li>
<li>
<p>为了实现 <code>DISCARD</code> 协议，该处理器必须忽略收到的消息。  <a href="http://netty.io/4.0/api/io/netty/buffer/ByteBuf.html" target="_blank" rel="external"><code>ByteBuf</code></a> 是一个引用计数对象，所以必须显式的调用 <code>release()</code> 方法来释放资源。请谨记，任何传递到处理器引用计数对象，处理器都有责任将其释放掉。通常， <code>channelRead()</code> 方法的实现如下：</p>
</li>
</ol>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="meta">@Override</span></div><div class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">channelRead</span><span class="params">(ChannelHandlerContext ctx, Object msg)</span> </span>&#123;</div><div class="line">    <span class="keyword">try</span> &#123;</div><div class="line">        <span class="comment">// Do something with msg</span></div><div class="line">    &#125; <span class="keyword">finally</span> &#123;</div><div class="line">        ReferenceCountUtil.release(msg);</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<ol start="4">
<li>当 Netty 犹豫 I/O 错误或者处理器处理事件时抛出异常，导致 Throwable 对象出现时，<code>exceptionCaught()</code> 方法被调用。大多数情况下，补货到的异常应该被记录，与其相关的 channle 也应该在这儿被关闭，当然了，该方法的实现也会因你的不同异常处理方案而不同。比如，你也许想在关闭连接之前发送一个包含错误代码的响应消息。</li>
</ol>
<p>目前来说一切都很好，我们实现了一半的 <code>DISCARD</code> 服务。现在剩下我们去在 <code>DiscardServerHandler</code> 中写一个 <code>main()</code> 方法来启动服务。</p>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div><div class="line">39</div><div class="line">40</div><div class="line">41</div><div class="line">42</div><div class="line">43</div><div class="line">44</div><div class="line">45</div><div class="line">46</div><div class="line">47</div><div class="line">48</div><div class="line">49</div><div class="line">50</div><div class="line">51</div><div class="line">52</div><div class="line">53</div><div class="line">54</div><div class="line">55</div><div class="line">56</div><div class="line">57</div><div class="line">58</div><div class="line">59</div><div class="line">60</div><div class="line">61</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">package</span> io.netty.example.discard;</div><div class="line"></div><div class="line"><span class="keyword">import</span> io.netty.bootstrap.ServerBootstrap;</div><div class="line"></div><div class="line"><span class="keyword">import</span> io.netty.channel.ChannelFuture;</div><div class="line"><span class="keyword">import</span> io.netty.channel.ChannelInitializer;</div><div class="line"><span class="keyword">import</span> io.netty.channel.ChannelOption;</div><div class="line"><span class="keyword">import</span> io.netty.channel.EventLoopGroup;</div><div class="line"><span class="keyword">import</span> io.netty.channel.nio.NioEventLoopGroup;</div><div class="line"><span class="keyword">import</span> io.netty.channel.socket.SocketChannel;</div><div class="line"><span class="keyword">import</span> io.netty.channel.socket.nio.NioServerSocketChannel;</div><div class="line"></div><div class="line"><span class="comment">/**</span></div><div class="line"> * 丢弃任何进来的数据。</div><div class="line"> */</div><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DiscardServer</span> </span>&#123;</div><div class="line"></div><div class="line">    <span class="keyword">private</span> <span class="keyword">int</span> port;</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="title">DiscardServer</span><span class="params">(<span class="keyword">int</span> port)</span> </span>&#123;</div><div class="line">        <span class="keyword">this</span>.port = port;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</div><div class="line">        EventLoopGroup bossGroup = <span class="keyword">new</span> NioEventLoopGroup(); <span class="comment">// (1)</span></div><div class="line">        EventLoopGroup workerGroup = <span class="keyword">new</span> NioEventLoopGroup();</div><div class="line">        <span class="keyword">try</span> &#123;</div><div class="line">            ServerBootstrap b = <span class="keyword">new</span> ServerBootstrap(); <span class="comment">// (2)</span></div><div class="line">            b.group(bossGroup, workerGroup)</div><div class="line">             .channel(NioServerSocketChannel.class) <span class="comment">// (3)</span></div><div class="line">             .childHandler(<span class="keyword">new</span> ChannelInitializer&lt;SocketChannel&gt;() &#123; <span class="comment">// (4)</span></div><div class="line">                 <span class="meta">@Override</span></div><div class="line">                 <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">initChannel</span><span class="params">(SocketChannel ch)</span> <span class="keyword">throws</span> Exception </span>&#123;</div><div class="line">                     ch.pipeline().addLast(<span class="keyword">new</span> DiscardServerHandler());</div><div class="line">                 &#125;</div><div class="line">             &#125;)</div><div class="line">             .option(ChannelOption.SO_BACKLOG, <span class="number">128</span>)          <span class="comment">// (5)</span></div><div class="line">             .childOption(ChannelOption.SO_KEEPALIVE, <span class="keyword">true</span>); <span class="comment">// (6)</span></div><div class="line"></div><div class="line">            <span class="comment">// 绑定端口并且开始服务接收进来的连接。</span></div><div class="line">            ChannelFuture f = b.bind(port).sync(); <span class="comment">// (7)</span></div><div class="line"></div><div class="line">            <span class="comment">// 等待 server socket 关闭。</span></div><div class="line">            <span class="comment">// 在这个例子中，这不会发生，但你可以这样优雅的关闭你的服务。</span></div><div class="line">            f.channel().closeFuture().sync();</div><div class="line">        &#125; <span class="keyword">finally</span> &#123;</div><div class="line">            workerGroup.shutdownGracefully();</div><div class="line">            bossGroup.shutdownGracefully();</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception </span>&#123;</div><div class="line">        <span class="keyword">int</span> port;</div><div class="line">        <span class="keyword">if</span> (args.length &gt; <span class="number">0</span>) &#123;</div><div class="line">            port = Integer.parseInt(args[<span class="number">0</span>]);</div><div class="line">        &#125; <span class="keyword">else</span> &#123;</div><div class="line">            port = <span class="number">8080</span>;</div><div class="line">        &#125;</div><div class="line">        <span class="keyword">new</span> DiscardServer(port).run();</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<ol>
<li>
<p><a href="http://netty.io/4.0/api/io/netty/channel/nio/NioEventLoopGroup.html" target="_blank" rel="external"><code>NioEventLoopGroup</code></a> 是一个用来处理 I/O 操作的多线程事件循环器。 Netty 为不同类型传输提供了各种各样的 <a href="http://netty.io/4.0/api/io/netty/channel/EventLoopGroup.html" target="_blank" rel="external"><code>EventLoopGroup</code></a> 的实现。这个例子我们实现了一个服务端的应用，声明了两个 <a href="http://netty.io/4.0/api/io/netty/channel/nio/NioEventLoopGroup.html" target="_blank" rel="external"><code>NioEventLoopGroup</code></a> 实例。第一个，通常称为 “boss”,用来接收进来的连接。第二个，通常称为 “worker”, 用来处理接收到连接，一旦 boss 接收到连接，就将其注册到 worker 上。使用多少个线程，如何将其映射到已经创建好的 <a href="http://netty.io/4.0/api/io/netty/channel/Channel.html" target="_blank" rel="external"><code>channel</code></a>S 依赖于 <a href="http://netty.io/4.0/api/io/netty/channel/EventLoopGroup.html" target="_blank" rel="external"><code>EventLoopGroup</code></a> 的实现，也可以通过构造方法进行配置。</p>
</li>
<li>
<p><a href="http://netty.io/4.0/api/io/netty/bootstrap/ServerBootstrap.html" target="_blank" rel="external"><code>ServerBootstrap</code></a> 是一个建立 server 的辅助类。你也可以通过 <a href="http://netty.io/4.0/api/io/netty/channel/Channel.html" target="_blank" rel="external"><code>Channel</code></a> 直接建立一个 server。但请注意，这是一个非常冗长的过程，很多时候，你并不需要这么做。</p>
</li>
<li>
<p>在这儿，我们指定使用 <a href="http://netty.io/4.0/api/io/netty/channel/socket/nio/NioServerSocketChannel.html" target="_blank" rel="external"><code>NioServerSocketChannel</code></a> 类来实例化一个新的 <a href="http://netty.io/4.0/api/io/netty/channel/Channel.html" target="_blank" rel="external"><code>Channel</code></a> 来接收进来的连接。</p>
</li>
<li>
<p>在这儿指定的处理器通常由新接收的 Channel 进行评估。<a href="http://netty.io/4.0/api/io/netty/channel/ChannelInitializer.html" target="_blank" rel="external"><code>ChannelInitializer</code></a> 是一个特殊的处理器，用来帮助用户配置一个新的 <a href="http://netty.io/4.0/api/io/netty/channel/Channel.html" target="_blank" rel="external"><code>Channel</code></a>。很可能你会想通过添加处理器,如 <code>DiscardServerHandler</code> ,来配置新 <a href="http://netty.io/4.0/api/io/netty/channel/Channel.html" target="_blank" rel="external"><code>Channel</code></a> 的 <a href="http://netty.io/4.0/api/io/netty/channel/ChannelPipeline.html" target="_blank" rel="external"><code>ChannelPipeline</code></a> ,用来实现你的网络应用。随着应用变得复杂，你会往管道添加更多的处理器，最终你可能会把这个匿名类提取为一个单独的类。</p>
</li>
<li>
<p>你可以设置指定 Channel 实现的特定参数。我们在写一个 TCP/IP 服务，所以我们可以设置一些 socket 的选项，比如 <code>tcpNoDelay</code> 和 <code>keepAlive</code> 。请参照 <a href="http://netty.io/4.0/api/io/netty/channel/ChannelOption.html" target="_blank" rel="external"><code>ChannelOption</code></a> API 文档以及特定的 <a href="http://netty.io/4.0/api/io/netty/channel/ChannelConfig.html" target="_blank" rel="external"><code>ChannelConfig</code></a> 实现，以获得有关 <code>ChannelOptionS</code> 支持的概述。</p>
</li>
<li>
<p>你有注意到 <code>option()</code> 和 <code>childOption</code> 方法了吗？ <code>option()</code> 是为 <a href="http://netty.io/4.0/api/io/netty/channel/socket/nio/NioServerSocketChannel.html" target="_blank" rel="external"><code>NioServerSocketChannel</code></a> 用来接收进来的连接。 <code>childOption()</code> 是为被父通道 <a href="http://netty.io/4.0/api/io/netty/channel/ServerChannel.html" target="_blank" rel="external"><code>ServerChannel</code></a> 接收到的 <code>ChannelS</code> ，在本例中指的是 <a href="http://netty.io/4.0/api/io/netty/channel/socket/nio/NioServerSocketChannel.html" target="_blank" rel="external"><code>NioServerSocketChannel</code></a>。</p>
</li>
<li>
<p>我们现在已经准备好了。剩下的就是绑定端口和启动服务了。在这儿，我们绑定了机器所有网卡的 <code>8080</code> 端口。你现在可以调用多次 <code>bind()</code> 方法（用不同的绑定地址）。</p>
</li>
</ol>
<p>恭喜你！你已经基于 Netty 完成里你的第一个服务端程序。</p>
<h2 id="查看接收到的数据looking-into-the-received-data">查看接收到的数据<a href="http://netty.io/wiki/user-guide-for-4.x.html#looking-into-the-received-data" target="_blank" rel="external">Looking into the Received Data</a></h2>
<p>现在我们已经写好了我们的第一个服务端程序，我们需要测试它是否正常运行。最简单的方法是使用 <code>telnet</code> 命令行。比如，你可以在终端输入 <code>telnet localhost 8080</code> 命令行，然后输入一些东西。</p>
<p>但这样就能表示我们的服务运行正常吗？我们实际上并不能准确知道，因为它是个 <code>discard</code> 服务。它并不会返回任何信息。为了证明它是真的正常运行，我们可以修改一下服务端程序让它把收到的数据打印出来。</p>
<p>我们已经知道当数据被接收到的时候会调用 <code>channelRead()</code> 方法。可以在 <code>DiscardServerHandler</code> 的 <code>channelRead()</code> 方法中加一些代码：</p>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line"><span class="meta">@Override</span></div><div class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">channelRead</span><span class="params">(ChannelHandlerContext ctx, Object msg)</span> </span>&#123;</div><div class="line">    ByteBuf in = (ByteBuf) msg;</div><div class="line">    <span class="keyword">try</span> &#123;</div><div class="line">        <span class="keyword">while</span> (in.isReadable()) &#123; <span class="comment">// (1)</span></div><div class="line">            System.out.print((<span class="keyword">char</span>) in.readByte());</div><div class="line">            System.out.flush();</div><div class="line">        &#125;</div><div class="line">    &#125; <span class="keyword">finally</span> &#123;</div><div class="line">        ReferenceCountUtil.release(msg); <span class="comment">// (2)</span></div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<ol>
<li>这个低效的循环实际上可以替换为: <code>System.out.println(in.toString(io.netty.util.CharsetUtil.US_ASCII))</code></li>
<li>或者，你可以在这儿调 <code>in.release()</code></li>
</ol>
<p>如果你再次调用 <code>telnet</code> 命令，你会看到服务端程序会把收到的数据打印出来。</p>
<p>discard 服务的全部代码放在 <a href="http://netty.io/4.0/xref/io/netty/example/discard/package-summary.html" target="_blank" rel="external"><code>io.netty.example.discard</code></a> 包中。</p>
<h2 id="写一个-echo-服务-writing-an-echo-server">写一个 Echo 服务 <a href="http://netty.io/wiki/user-guide-for-4.x.html#writing-an-echo-server" target="_blank" rel="external">Writing an Echo Server</a></h2>
<p>目前为止，我们的服务都是消费数据，而不做任何响应。然而作为一个服务，往往是需要对请求做出响应的。让我们学习如何通过实现 <a href="http://tools.ietf.org/html/rfc862" target="_blank" rel="external"><code>ECHO</code></a> 协议（如论接收到任何数据都原样返回）来给客户端返回响应数据。</p>
<p>与我们先前小节中实现的 discard 服务唯一不同的是，我们将接收到的数据发出，而不是将其打印到控制台。因此，需要再次改动 <code>channelRead()</code> 方法：</p>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="meta">@Override</span></div><div class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">channelRead</span><span class="params">(ChannelHandlerContext ctx, Object msg)</span> </span>&#123;</div><div class="line">   ctx.write(msg); <span class="comment">// (1)</span></div><div class="line">   ctx.flush(); <span class="comment">// (2)</span></div><div class="line">&#125;</div></pre></td></tr></table></figure>
<ol>
<li>
<p><a href="http://netty.io/4.0/api/io/netty/channel/ChannelHandlerContext.html" target="_blank" rel="external"><code>ChannelHandlerContext</code></a> 对象提供了各种操作可以用来触发各种 I/O 事件以及操作。在这儿我们调用 <code>write(Object)</code> 方法将接收到的消息一字不差的返回。请注意，在这儿我们没有像 <code>DISCARD</code> 服务一样 release 接收到的消息，因为 Netty 会在你将其写出至网络时将其 release。</p>
</li>
<li>
<p><code>ctx.write(Object)</code> 并不会将消息写到网络上。它只是被放在内部缓冲区，然后被 <code>ctx.flush()</code> 方法刷新至网络上。或者你可以用更简洁的 <code>ctx.writeAndFlush(msg)</code> 方法。</p>
</li>
</ol>
<p>如果你再次运行 <code>telnet</code> 命令，你会发现无论你发送什么内容到服务器上，它都会将其原样返回。</p>
<p>echo 服务的全部代码可以在 <a href="http://netty.io/4.0/xref/io/netty/example/echo/package-summary.html" target="_blank" rel="external"><code>io.netty.example.echo</code></a> 包中找到。</p>
<h2 id="写一个时间服务-writing-a-time-server">写一个时间服务 <a href="http://netty.io/wiki/user-guide-for-4.x.html#writing-a-time-server" target="_blank" rel="external">Writing a Time Server</a></h2>
<p>本小节需要实现的协议是 <a href="http://tools.ietf.org/html/rfc868" target="_blank" rel="external"><code>TIME</code></a> 协议。与之前例子不同的是，它不会接收任何请求，会发送一条消息，里面包含了一个 32 位数字，当消息发送之后，会关闭连接。在本例中，你将了解到如何构造并发送一条消息，并且在服务结束的时候关闭连接。</p>
<p>因为你不打算接受任何消息，而连接一旦建立，你就会将消息发出，所以我们这次不能使用 <code>channelRead()</code> 方法。我们需要重写 <code>channelActive()</code> 方法。以下为实现：</p>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">package</span> io.netty.example.time;</div><div class="line"></div><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TimeServerHandler</span> <span class="keyword">extends</span> <span class="title">ChannelInboundHandlerAdapter</span> </span>&#123;</div><div class="line"></div><div class="line">    <span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">channelActive</span><span class="params">(<span class="keyword">final</span> ChannelHandlerContext ctx)</span> </span>&#123; <span class="comment">// (1)</span></div><div class="line">        <span class="keyword">final</span> ByteBuf time = ctx.alloc().buffer(<span class="number">4</span>); <span class="comment">// (2)</span></div><div class="line">        time.writeInt((<span class="keyword">int</span>) (System.currentTimeMillis() / <span class="number">1000L</span> + <span class="number">2208988800L</span>));</div><div class="line"></div><div class="line">        <span class="keyword">final</span> ChannelFuture f = ctx.writeAndFlush(time); <span class="comment">// (3)</span></div><div class="line">        f.addListener(<span class="keyword">new</span> ChannelFutureListener() &#123;</div><div class="line">            <span class="meta">@Override</span></div><div class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">operationComplete</span><span class="params">(ChannelFuture future)</span> </span>&#123;</div><div class="line">                <span class="keyword">assert</span> f == future;</div><div class="line">                ctx.close();</div><div class="line">            &#125;</div><div class="line">        &#125;); <span class="comment">// (4)</span></div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">exceptionCaught</span><span class="params">(ChannelHandlerContext ctx, Throwable cause)</span> </span>&#123;</div><div class="line">        cause.printStackTrace();</div><div class="line">        ctx.close();</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<ol>
<li>
<p>解释一下， <code>channelActive()</code> 方法会在连接建立好可以准备交换数据时被调用。我们在这儿写一个可以表示当前时间的 32 位数字。</p>
</li>
<li>
<p>为了发送一条新消息，我们需要分配一个新的缓冲区 (buffer) 用来存放消息。我们将要写一个 32 位的数字，因此，需要一个容量至少为 4 个字节的 <a href="http://netty.io/4.0/api/io/netty/buffer/ByteBuf.html" target="_blank" rel="external"><code>ByteBuf</code></a> 。通过 <code>ChannelHandlerContext.alloc()</code> 方法获取当前的 <a href="http://netty.io/4.0/api/io/netty/buffer/ByteBufAllocator.html" target="_blank" rel="external"><code>ByteBufAllocator</code></a> 对象并且分配一个新的缓冲区 (buffer)。</p>
</li>
<li>
<p>像往常一样，我们写构造的数据。<br>
但等等， flip 方法呢？ 我们在 NIO 中发送消息之前不需要调用 <code>java.nio.ByteBuffer.flip()</code> 方法吗？ <code>ByteBuf</code> 没有这个方法，因为它有两个指针: 一个是用来读操作，另一个是用来写操作。当你往 <code>ByteBuf</code> 写入数据的时候，写指针(writer index)会变大，而读指针(reader index)没有变化。读指针(reader index) 和 写指针(writer index) 分别体现了消息的开始和结尾位置。<br>
对比 NIO 缓冲区，其并没有提供一个清晰的方法可以计算出消息的开始和结束位置，除了调用 flip 方法。当你忘记 flip 缓冲区时，你会遇到麻烦，因为有可能发不出数据或者发错数据。而这样的错误不会在 Netty 发生，因为针对不同的操作，我们有不同的指针。你会发现习惯了没有 flipping out 的生活变得更简单！<br>
另一个需要注意的点是, <code>ChannelHandlerContext.write()</code> (以及 <code>writeAndFlush()</code>) 方法返回一个 <a href="http://netty.io/4.0/api/io/netty/channel/ChannelFuture.html" target="_blank" rel="external"><code>ChannelFuture</code></a> 对象。一个 <a href="http://netty.io/4.0/api/io/netty/channel/ChannelFuture.html" target="_blank" rel="external"><code>ChannelFuture</code></a> 对象代表一个 I/O 操作还没发生。这意味着任何请求操作都有可能尚未执行，因为 Netty 中所有的操作都是异步的。比如，如下例子中，有可能在消息发出去之前关闭连接:</p>
 <figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">Channel ch = ...;</div><div class="line">ch.writeAndFlush(message);</div><div class="line">ch.close();</div></pre></td></tr></table></figure>
<p>因此你需要在 <code>write()</code> 方法返回的 <a href="http://netty.io/4.0/api/io/netty/channel/ChannelFuture.html" target="_blank" rel="external"><code>ChannelFuture</code></a> 对象完成之后关闭连接，当写操作完成后，它会通知它的监听程序。请注意，<code>close()</code> 方法也有可能不会立即关闭连接，他也会返回一个 <a href="http://netty.io/4.0/api/io/netty/channel/ChannelFuture.html" target="_blank" rel="external"><code>ChannelFuture</code></a> 对象。</p>
</li>
<li>
<p>当写请求完成时我们如何得到通知呢? 很简单，只需要对返回的 <code>ChannelFuture</code> 添加 <a href="http://netty.io/4.0/api/io/netty/channel/ChannelFutureListener.html" target="_blank" rel="external"><code>ChannelFutureListener</code></a> 即可。在这儿我们创建了一个新的匿名类 <a href="http://netty.io/4.0/api/io/netty/channel/ChannelFutureListener.html" target="_blank" rel="external"><code>ChannelFutureListener</code></a> , 当操作完成时它会关闭 <code>Channel</code>。<br>
要不然你也可以使用预定义的监听器来简化你的代码：</p>
 <figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">f.addListener(ChannelFutureListener.CLOSE);</div></pre></td></tr></table></figure>
</li>
</ol>
<p>可以使用 UNIX 命令 <code>rdate</code> 来测试我们的时间服务是否能如预期工作：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">$ rdate -o &lt;port&gt; -p &lt;host&gt;</div></pre></td></tr></table></figure>
<p>这儿的 <code>&lt;port&gt;</code> 是在 <code>main()</code> 方法中指定的端口号，<code>&lt;host&gt;</code> 通常是 <code>localhost</code> 。</p>
<h2 id="写一个时间客户端-writing-a-time-client">写一个时间客户端 <a href="https://github.com/netty/netty/wiki/User-guide-for-4.x#writing-a-time-client" target="_blank" rel="external">Writing a Time Client</a></h2>
<p>不想 <code>DISCARD</code> 和 <code>ECHO</code> 服务， <code>TIME</code> 服务需要一个客户端。因为普通人很难将 32 为二进制数据翻译为日历上对应的时间。在本小节，我们将讨论如何确保服务正确的运行并且学习如何用 Netty 写一个客户端。</p>
<p>Netty 服务端和客户端最大也是唯一的区别就在于使用不同的 <a href="http://netty.io/4.0/api/io/netty/bootstrap/Bootstrap.html" target="_blank" rel="external"><code>bootstrap</code></a> 和 <a href="http://netty.io/4.0/api/io/netty/channel/Channel.html" target="_blank" rel="external"><code>Channel</code></a> 实现。请注意观察以下代码：</p>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">package</span> io.netty.example.time;</div><div class="line"></div><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TimeClient</span> </span>&#123;</div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception </span>&#123;</div><div class="line">        String host = args[<span class="number">0</span>];</div><div class="line">        <span class="keyword">int</span> port = Integer.parseInt(args[<span class="number">1</span>]);</div><div class="line">        EventLoopGroup workerGroup = <span class="keyword">new</span> NioEventLoopGroup();</div><div class="line"></div><div class="line">        <span class="keyword">try</span> &#123;</div><div class="line">            Bootstrap b = <span class="keyword">new</span> Bootstrap(); <span class="comment">// (1)</span></div><div class="line">            b.group(workerGroup); <span class="comment">// (2)</span></div><div class="line">            b.channel(NioSocketChannel.class); <span class="comment">// (3)</span></div><div class="line">            b.option(ChannelOption.SO_KEEPALIVE, <span class="keyword">true</span>); <span class="comment">// (4)</span></div><div class="line">            b.handler(<span class="keyword">new</span> ChannelInitializer&lt;SocketChannel&gt;() &#123;</div><div class="line">                <span class="meta">@Override</span></div><div class="line">                <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">initChannel</span><span class="params">(SocketChannel ch)</span> <span class="keyword">throws</span> Exception </span>&#123;</div><div class="line">                    ch.pipeline().addLast(<span class="keyword">new</span> TimeClientHandler());</div><div class="line">                &#125;</div><div class="line">            &#125;);</div><div class="line"></div><div class="line">            <span class="comment">// 打开客户端</span></div><div class="line">            ChannelFuture f = b.connect(host, port).sync(); <span class="comment">// (5)</span></div><div class="line"></div><div class="line">            <span class="comment">// 等待连接关闭</span></div><div class="line">            f.channel().closeFuture().sync();</div><div class="line">        &#125; <span class="keyword">finally</span> &#123;</div><div class="line">            workerGroup.shutdownGracefully();</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<ol>
<li>
<p><a href="http://netty.io/4.0/api/io/netty/bootstrap/Bootstrap.html" target="_blank" rel="external"><code>bootstrap</code></a> 与 <a href="http://netty.io/4.0/api/io/netty/bootstrap/ServerBootstrap.html" target="_blank" rel="external"><code>ServerBootstrap</code></a> 很类似，除了它使用的是非服务端通道 (non-server channels), 如客户端或者无连接通道。</p>
</li>
<li>
<p>如果你只指定了一个 <a href="http://netty.io/4.0/api/io/netty/channel/EventLoopGroup.html" target="_blank" rel="external"><code>EventLoopGroup</code></a> ，它将会既当作 boss 组又会当作 worker 组。虽然客户端并不使用 boss worker 。</p>
</li>
<li>
<p><a href="http://netty.io/4.0/api/io/netty/channel/socket/nio/NioSocketChannel.html" target="_blank" rel="external"><code>NioSocketChannel</code></a> 替换 <a href="http://netty.io/4.0/api/io/netty/channel/socket/nio/NioServerSocketChannel.html" target="_blank" rel="external"><code>NioServerSocketChannel</code></a> , 用来创建客户端的 <a href="http://netty.io/4.0/api/io/netty/channel/Channel.html" target="_blank" rel="external"><code>Channel</code></a> 。</p>
</li>
<li>
<p>留意我们并没有在这儿像 <code>ServerBootstrap</code> 一样使用 <code>childOption()</code> 方法，因为客户端的 <a href="http://netty.io/4.0/api/io/netty/channel/socket/SocketChannel.html" target="_blank" rel="external"><code>SocketChannel</code></a> 没有父。</p>
</li>
<li>
<p>与服务端调用 <code>bind()</code> 不同，我们应该在客户端调用 <code>connect()</code> 方法。</p>
</li>
</ol>
<p>如你所见，客户端的代码并没有与服务端差太多。那么 <a href="http://netty.io/4.0/api/io/netty/channel/ChannelHandler.html" target="_blank" rel="external"><code>ChannelHandler</code></a> 该如何实现呢？它应该从服务端接收到 32 为的整数，将其翻译为人类可读的格式并将其打印出来，然后关闭连接：</p>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">package</span> io.netty.example.time;</div><div class="line"></div><div class="line"><span class="keyword">import</span> java.util.Date;</div><div class="line"></div><div class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TimeClientHandler</span> <span class="keyword">extends</span> <span class="title">ChannelInboundHandlerAdapter</span> </span>&#123;</div><div class="line">    <span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">channelRead</span><span class="params">(ChannelHandlerContext ctx, Object msg)</span> </span>&#123;</div><div class="line">        ByteBuf m = (ByteBuf) msg; <span class="comment">// (1)</span></div><div class="line">        <span class="keyword">try</span> &#123;</div><div class="line">            <span class="keyword">long</span> currentTimeMillis = (m.readUnsignedInt() - <span class="number">2208988800L</span>) * <span class="number">1000L</span>;</div><div class="line">            System.out.println(<span class="keyword">new</span> Date(currentTimeMillis));</div><div class="line">            ctx.close();</div><div class="line">        &#125; <span class="keyword">finally</span> &#123;</div><div class="line">            m.release();</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="meta">@Override</span></div><div class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">exceptionCaught</span><span class="params">(ChannelHandlerContext ctx, Throwable cause)</span> </span>&#123;</div><div class="line">        cause.printStackTrace();</div><div class="line">        ctx.close();</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<ol>
<li>在 TCP/IP 中，Netty 将发送来的数据读到 <a href="http://netty.io/4.0/api/io/netty/buffer/ByteBuf.html" target="_blank" rel="external"><code>ByteBuf</code></a> 中。</li>
</ol>
<p>看上去很简单，并且没有发现什么与服务端例子中不同的地方。然而，这个处理器有时候会因为引发 <code>IndexOutOfBoundsException</code> 异常，而拒绝工作。我们将在下一小节讨论这为什么会发生。</p>
<p><em>&lt;未完待续&gt;</em></p>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;Based on &lt;a href=&quot;http://netty.io/wiki/user-guide-for-4.x.html&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;User guide for 4.x&lt;/a&gt; .&lt;/p&gt;

    
    </summary>
    
      <category term="文档" scheme="https://mahui.github.io/categories/%E6%96%87%E6%A1%A3/"/>
    
    
      <category term="Netty" scheme="https://mahui.github.io/tags/Netty/"/>
    
      <category term="Java" scheme="https://mahui.github.io/tags/Java/"/>
    
      <category term="翻译" scheme="https://mahui.github.io/tags/%E7%BF%BB%E8%AF%91/"/>
    
  </entry>
  
  <entry>
    <title>为什么要造轮子</title>
    <link href="https://mahui.github.io/2017/03/06/Why-did-I-create-an-ORM-framework-again/"/>
    <id>https://mahui.github.io/2017/03/06/Why-did-I-create-an-ORM-framework-again/</id>
    <published>2017-03-06T14:28:36.000Z</published>
    <updated>2017-03-06T14:55:01.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>当我在 <a href="http://oschina.net" target="_blank" rel="external">oschina.net</a> 上以 java orm 作为关键词进行搜索的时候，共找到529条结果。那么我为什么还是要写 ur-orm 呢？</p>
</blockquote>
<ul>
<li>与DB交互的时候，只想调用一个方法就行了</li>
<li>虽然看上去不够高大上，但在代码里写sql感觉就是舒服呀😌</li>
<li>与查询的时候返回个对象用起来遍历，当然了，这也是 ORM 框架最起码要实现的</li>
<li>轻量级，轻量级，还是轻量级。在大家都在说轻量级的时候，要做到真正但轻量级，换ORM的时候，只会觉得当前的代码不够用，而不是发现当前的一堆配置，代码是多余的，对另一个框架来说。当然了，这有点儿偏激。</li>
<li>重要的是，要对味儿。
<ul>
<li>用了好多年的 MyBatis 了，至今还是觉得这是个好的框架，但实在不想在项目开始的时候先来一拖它的配置，不管是 resultMap 还是其它。</li>
<li>JPA 让 Java 开发有了点儿敏捷的意思了，但有时候封装的太深，用起来让人不知所措。</li>
</ul>
</li>
</ul>
<blockquote>
<p>当然了，这个轮子，说不定哪天也会变得让人又爱又恨。😄</p>
</blockquote>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;当我在 &lt;a href=&quot;http://oschina.net&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;oschina.net&lt;/a&gt; 上以 java orm 作为关键词进行搜索的时候，共找到529条结果。那么我为什么还是要
    
    </summary>
    
      <category term="日记" scheme="https://mahui.github.io/categories/%E6%97%A5%E8%AE%B0/"/>
    
    
      <category term="杂记" scheme="https://mahui.github.io/tags/%E6%9D%82%E8%AE%B0/"/>
    
  </entry>
  
  <entry>
    <title>要做的事情</title>
    <link href="https://mahui.github.io/2017/03/03/Something-to-do/"/>
    <id>https://mahui.github.io/2017/03/03/Something-to-do/</id>
    <published>2017-03-02T17:20:16.000Z</published>
    <updated>2017-03-06T14:26:39.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>有很多事情要做，把他们列下来</p>
</blockquote>
<h1 id="side-project">side project</h1>
<ul>
<li>ur-orm</li>
</ul>
<h1 id="学习">学习</h1>
<ul>
<li>vue.js</li>
<li>netty</li>
<li>英语</li>
</ul>
<h1 id="读书">读书</h1>
<ul>
<li>三体</li>
<li>java虚拟机规范</li>
<li>响应式web设计</li>
</ul>
<h1 id="产出">产出</h1>
<ul>
<li>git练习代码提交</li>
<li>学习笔记</li>
</ul>
<h1 id="为什么要学前端">为什么要学前端</h1>
<p>作为一个一直在写后段代码的人，有着一颗产品的❤️，想要把自己的idea展示出来可不是看着 terminal 中应用启动成功了就行了的。</p>
<blockquote>
<p>时间过的很快，2017年的1/6又没了</p>
</blockquote>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;有很多事情要做，把他们列下来&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h1 id=&quot;side-project&quot;&gt;side project&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;ur-orm&lt;/li&gt;
&lt;/ul&gt;
&lt;h1 id=&quot;学习&quot;&gt;学习&lt;/h1&gt;
&lt;ul&gt;
    
    </summary>
    
      <category term="日记" scheme="https://mahui.github.io/categories/%E6%97%A5%E8%AE%B0/"/>
    
    
      <category term="杂记" scheme="https://mahui.github.io/tags/%E6%9D%82%E8%AE%B0/"/>
    
  </entry>
  
  <entry>
    <title>Hexo部署笔记</title>
    <link href="https://mahui.github.io/2017/03/01/Hexo-deploy-note/"/>
    <id>https://mahui.github.io/2017/03/01/Hexo-deploy-note/</id>
    <published>2017-03-01T08:10:54.000Z</published>
    <updated>2017-03-02T17:34:22.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="主题选择">主题选择</h1>
<ul>
<li><a href="https://github.com/pinggod/hexo-theme-apollo" title="apollo" target="_blank" rel="external">hexo-theme-apollo</a></li>
<li>网上找了个水滴的图标，好看，一点点寓意。</li>
</ul>
<h1 id="多客户端写文章的问题">多客户端写文章的问题</h1>
<p><code>hexo d</code> 执行后 <em>hexo</em> 将 <code>hexo g</code> 生成的 <em>public</em> 文件夹下的静态文件部署到目标服务器，其它 <em>hexo</em> 环境相关的内容则还在本机上，这样势必导致之前辛苦装的一系列插件，主题之类的东西没有办法同步到其它机器上，更换电脑是件麻烦事。</p>
<p>暂且想到集中解决方案</p>
<ul>
<li>使用 dropbox，OneDriver 之类的文件同步服务同步源文件</li>
<li>另外建一个repo同步源文件</li>
<li>网络上还有一种将源文件提交到新分支的做法</li>
</ul>
<p>看了<code>hexo init</code> 后的 <em>blog_dir</em> ,显然 <em>hexo</em> 已经考虑到这方面的问题，支持将源文件提交到 <em>git</em> 仓库中，因为 <em>.gitignore</em> 已经初始化好了。</p>
<p>作为一个稍微有点儿强迫症的狮子座，决定采用新建分支的方法</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">cd $blog_dir</div><div class="line">git init</div><div class="line">git add *</div><div class="line">git checkout -b $branch_name</div><div class="line">git remote add origin git@github.com:xxxx/xxxx.github.io.git</div><div class="line">git push origin $branch_name:$branch_name</div></pre></td></tr></table></figure>
<p>需要注意的是，<em>_config.yml</em> 中 deploy 节点不要暴露敏感信息。
在新的客户端，只需要将代码clone下来，<code>checkout</code>到相应分支，<code>npm install -g hexo-cli</code>,<code>npm install</code> 之后 <code>new</code>, <code>generate</code>, <code>deploy</code> 即可</p>
<p>关于源码的维护，可以将当前 repo 的 default branch 设置为源码的分支，以方便后续维护。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;主题选择&quot;&gt;主题选择&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://github.com/pinggod/hexo-theme-apollo&quot; title=&quot;apollo&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;hexo-t
    
    </summary>
    
      <category term="笔记" scheme="https://mahui.github.io/categories/%E7%AC%94%E8%AE%B0/"/>
    
    
      <category term="hexo" scheme="https://mahui.github.io/tags/hexo/"/>
    
      <category term="部署文档" scheme="https://mahui.github.io/tags/%E9%83%A8%E7%BD%B2%E6%96%87%E6%A1%A3/"/>
    
  </entry>
  
</feed>
